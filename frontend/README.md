# Frontend

Nuxt 3 frontend для тестового задания Favorites SPA.

## Docker

Frontend запускается через корневой `docker-compose.yml`.

```powershell
docker-compose up -d --build
```

## Проверки

```powershell
docker-compose build frontend
```
